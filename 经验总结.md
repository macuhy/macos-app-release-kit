# 经验总结（实战踩坑 & 可复用清单）

> 来自 `macuhy/kown` 首次接入本套件、从 0 跑通 CI 发布的全过程。以下是**真实踩到的坑**与**可直接复用的结论**。配套实例细节见 `docs/06-本实例配置与踩坑.md`。

## 一、五个必踩的坑（及修复）

| # | 现象 | 根因 | 修复（已固化进 kit） |
|---|------|------|----------------------|
| 1 | run **1 秒 `startup_failure`**，无 job | 新建组织/仓库默认 `GITHUB_TOKEN` 只读，reusable workflow 要 `contents: write`，被调用方权限不能超过调用方 | 调用方加顶层 `permissions: contents: write` |
| 2 | `Secret X is required, but not provided` | **跨仓库**调用时 `secrets: inherit` 不转发组织级 secret | 调用方**显式逐个传** `NAME: ${{ secrets.NAME }}` |
| 3 | `SecKeychainItemImport: Unable to decode`（证书解码 0 字节） | **GitHub Free 计划的组织级 secret 不能被私有仓库使用**（需 Team/Enterprise） | 私有仓库用 **repo 级 secret**；要组织级须升级 Team |
| 4 | Archive 报签名冲突 / `requires a provisioning profile with the iCloud feature` | project.yml 是 `Automatic` 却手动指定 Developer ID；且 iCloud 等受限 entitlement 必须有 profile | archive 用 `CODE_SIGN_STYLE=Manual` + `PROVISIONING_PROFILE_SPECIFIER`；ExportOptions 用 `signingStyle=manual` + profile 映射；profile 存 `PROVISIONING_PROFILE_BASE64` secret 并装到 `~/Library/MobileDevice/Provisioning Profiles/` |
| 5 | CI 里 `xcodegen generate` 找不到 spec | spec（`mac/project.yml`）在子目录，CI 在根目录跑 | 本地 `xcodegen generate` 生成并**提交** xcodeproj，调用方 `use_xcodegen: false` |

## 二、关键认知

- **secret 在"运行 workflow 的仓库"解析**，不是存放 reusable workflow 的仓库。公共 kit 仓库放逻辑、零泄露；密钥放调用方（私有 App 仓库或组织级）。
- **"配一次到处用"= 组织级 secret = 需付费 Team**。Free + 私有仓库只能逐仓库设 secret。
- **带 iCloud/Push 等受限 entitlement 的 App** 必须走 manual 签名 + 提供 Developer ID provisioning profile（普通 App 无需）。
- **签名/公证只能在已签名+公证的产物上验证自动更新**；未签名本地构建测不出 Sparkle 静默安装。

## 三、接入新私有 App 的清单

1. 建私有源码仓库 + 公共分发仓库（后者先放一个初始 `appcast.xml` 占位）。
2. 调用方 `.github/workflows/release.yml`（抄 `templates/caller-workflow.yml`）：
   - 顶层 `permissions: contents: write`（坑 1）
   - `secrets:` **显式逐个传**（坑 2）
   - `with:` 填 app_name/scheme/project/public_repo/download_url_prefix
3. 设 secret：私有仓库 **repo 级**（Free）或组织级（Team）。
4. 证书：`security export ... -f pkcs12` 导出 → `base64 -i x.p12 | tr -d '\n'` → secret；用 openssl 验证含 Developer ID Application + 私钥。
5. 受限 entitlement（iCloud 等）：填 `provisioning_profile_specifier` + `PROVISIONING_PROFILE_BASE64` secret + ExportOptions 改 manual（坑 4）。
6. App 端：Sparkle 依赖（macOS 条件）+ Info.plist 填 `SUFeedURL`/`SUPublicEDKey` + 接 UpdaterService/菜单。
7. 触发：`git tag vX.Y.Z && git push origin vX.Y.Z`。
